import { stateLayerOpacity } from "@/styles/stateLayer.css";
import { theme } from "@/styles/theme.css";
import { typography } from "@/styles/typography.css";

const tokens = { stateLayerOpacity, ...theme, typography };

export default tokens;
